# Cashu Mint Configuration
# This is the URL of the Cashu mint to use for token verification and minting
NEXT_PUBLIC_CASHU_MINT_URL=https://testnut.cashu.space

# Game Configuration
# Maximum bet allowed in satoshis
MAX_BET_SATS=1000

# Minimum bet allowed in satoshis
MIN_BET_SATS=1

# Admin API Key
# CRITICAL: Generate a secure random key for production!
# Example: openssl rand -hex 32
ADMIN_API_KEY=your-secure-random-key-here

# Nostr Configuration
# Casino's Nostr private key (hex format) for sending DMs
# CRITICAL: Generate a secure random key for production!
# Example: openssl rand -hex 32
# Or use a Nostr key generator
CASINO_NOSTR_PRIVATE_KEY=your-nostr-private-key-hex-here

# Optional: Specific Nostr relays to use (comma-separated)
# If not set, uses default relays
# NOSTR_RELAYS=wss://relay.damus.io,wss://relay.nostr.band,wss://nos.lol
